# 🚀 دليل التنفيذ السريع - MVP (ساعتين)

## ✅ الخطوة 1: قاعدة البيانات (30 دقيقة)

### 1.1 افتح Supabase Dashboard
1. اذهب إلى [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. اختر مشروعك
3. من القائمة الجانبية، اختر **SQL Editor**

### 1.2 نفّذ ملف SQL
1. افتح ملف: `database/migrations/mvp_notification_system.sql`
2. انسخ **كل المحتوى**
3. الصقه في SQL Editor
4. اضغط **Run** أو **Ctrl+Enter**

### 1.3 تحقق من النجاح
نفّذ هذا الاستعلام للتحقق:
```sql
-- يجب أن يُرجع الجدولين
select table_name 
from information_schema.tables 
where table_schema = 'public' 
and table_name in ('notification_preferences', 'email_log');
```

✅ **النتيجة المتوقعة**: يجب أن ترى جدولين:
- `notification_preferences`
- `email_log`

---

## ✅ الخطوة 2: متغيرات البيئة (5 دقائق)

### 2.1 افتح ملف .env
افتح ملف `.env` في جذر المشروع

### 2.2 أضف متغيرات Zoho SMTP
```env
# Zoho SMTP Configuration
ZOHO_SMTP_HOST=smtp.zoho.com
ZOHO_SMTP_PORT=465
ZOHO_SMTP_SECURE=true
ZOHO_SMTP_USER=info@bacuratec.com
ZOHO_SMTP_PASS=20Bac30@
ZOHO_FROM_EMAIL=info@bacuratec.com
ZOHO_FROM_NAME=Bacura Platform
```

### 2.3 احفظ الملف
اضغط **Ctrl+S** لحفظ

⚠️ **مهم**: لا ترفع ملف `.env` إلى Git!

---

## ✅ الخطوة 3: تثبيت المكتبات (5 دقائق)

### 3.1 افتح Terminal
في VS Code، اضغط **Ctrl+`** (Backtick)

### 3.2 ثبّت Nodemailer
```bash
npm install nodemailer
```

### 3.3 انتظر حتى ينتهي التثبيت
✅ يجب أن ترى: `added 1 package`

---

## ✅ الخطوة 4: ملفات الكود (تم إنشاؤها مسبقاً!)

الملفات التالية تم إنشاؤها تلقائياً:

### 4.1 خدمة البريد الإلكتروني
✅ `src/services/emailService.js` - جاهز!

### 4.2 خدمة التحقق من التفضيلات
سننشئها الآن...

---

## ✅ الخطوة 5: إنشاء باقي الملفات (20 دقيقة)

### 5.1 ملف التحقق من التفضيلات
سيتم إنشاؤه في الخطوة التالية...

### 5.2 ملف خدمة الإشعارات
سيتم إنشاؤه في الخطوة التالية...

### 5.3 صفحة إعدادات الإشعارات (React)
سيتم إنشاؤها في الخطوة التالية...

---

## ✅ الخطوة 6: الاختبار (10 دقائق)

### 6.1 اختبار إرسال بريد
سننشئ ملف اختبار بسيط

### 6.2 اختبار صفحة الإعدادات
افتح المتصفح وجرّب الصفحة

---

## 📋 قائمة التحقق

- [x] ✅ تنفيذ SQL في Supabase
- [x] ✅ إضافة متغيرات البيئة
- [x] ✅ تثبيت Nodemailer
- [x] ✅ إنشاء emailService.js
- [ ] ⏳ إنشاء canSendEmail.js (التالي)
- [ ] ⏳ إنشاء notificationService.js (التالي)
- [ ] ⏳ إنشاء صفحة الإعدادات (التالي)
- [ ] ⏳ الاختبار

---

## 🆘 حل المشاكل

### المشكلة: خطأ في SQL
**الحل**: تأكد من نسخ الملف كاملاً، وأن Supabase متصل

### المشكلة: Nodemailer لا يعمل
**الحل**: تحقق من متغيرات البيئة، وأن App Password صحيح

### المشكلة: RLS يمنع الوصول
**الحل**: تأكد من تسجيل الدخول، أو استخدم Service Role Key

---

## ⏱️ الوقت المستغرق حتى الآن

- ✅ قاعدة البيانات: 30 دقيقة
- ✅ متغيرات البيئة: 5 دقائق
- ✅ تثبيت المكتبات: 5 دقيقة
- ✅ **الإجمالي**: 40 دقيقة

**⏳ المتبقي**: 80 دقيقة (ساعة و20 دقيقة)

---

## 🎯 الخطوة التالية

الآن سننشئ باقي ملفات الكود:
1. `canSendEmail.js` - للتحقق من التفضيلات
2. `notificationService.js` - لإرسال الإشعارات
3. `NotificationSettings.jsx` - صفحة الإعدادات

**جاهز للمتابعة؟** 🚀
