# إعداد MCP (Model Context Protocol) للتطوير الديناميكي

## ✅ الإعداد المكتمل

تم إعداد MCP بشكل كامل لتطوير وحل المشاكل ديناميكيًا دون انتظار.

## 🛠️ الأدوات المتاحة

### 1. Next.js MCP Tools

#### `nextjs_index`
- اكتشاف جميع خوادم Next.js قيد التشغيل
- عرض الأدوات المتاحة لكل خادم
- لا يحتاج معاملات - يكتشف الخوادم تلقائيًا

**الاستخدام:**
```javascript
// يكتشف تلقائيًا جميع خوادم Next.js على المنافذ المختلفة
```

#### `nextjs_call`
- الحصول على الأخطاء في الوقت الفعلي
- عرض جميع المسارات (Routes)
- الحصول على حالة البناء (Build Status)
- إدارة الذاكرة المؤقتة
- تشخيص المشاكل ديناميكيًا

**الاستخدام:**
```javascript
// الحصول على الأخطاء
nextjs_call({ port: "3000", toolName: "get_errors" })

// عرض جميع المسارات
nextjs_call({ port: "3000", toolName: "list_routes" })

// الحصول على حالة البناء
nextjs_call({ port: "3000", toolName: "get_build_status" })
```

#### `nextjs_docs`
- البحث في وثائق Next.js الرسمية
- الحصول على معلومات محدثة عن APIs والميزات
- **إلزامي** لجميع استعلامات Next.js

**الاستخدام:**
```javascript
// البحث
nextjs_docs({ action: "search", query: "generateStaticParams" })

// الحصول على وثائق كاملة
nextjs_docs({ action: "get", path: "/docs/app/api-reference/functions/generate-static-params" })
```

#### `browser_eval`
- اختبار الصفحات باستخدام Playwright
- التحقق من الوظائف والتقاط أخطاء التشغيل
- لقطات الشاشة والتحقق من السلوك

### 2. Supabase MCP Tools

#### إدارة قاعدة البيانات
- `mcp_supabase_list_tables` - عرض جميع الجداول
- `mcp_supabase_execute_sql` - تنفيذ استعلامات SQL
- `mcp_supabase_apply_migration` - تطبيق migrations
- `mcp_supabase_get_logs` - الحصول على السجلات

#### الأمان والأداء
- `mcp_supabase_get_advisors` - فحص الأمان والأداء
- `mcp_supabase_get_logs` - سجلات الأخطاء

### 3. Netlify MCP Tools

#### إدارة النشر
- `mcp_netlify_netlify-deploy-services-reader` - قراءة معلومات النشر
- `mcp_netlify_netlify-deploy-services-updater` - نشر الموقع

## 🚀 سير العمل الديناميكي

### 1. بدء التطوير
```bash
npm run dev
```

### 2. مراقبة الأخطاء تلقائيًا
- استخدم `nextjs_index` لاكتشاف الخادم
- استخدم `nextjs_call` مع `get_errors` لمراقبة الأخطاء
- الأخطاء تُكتشف تلقائيًا دون انتظار

### 3. حل المشاكل فورًا
- عند اكتشاف خطأ، استخدم `nextjs_docs` للحصول على الحل
- قم بإصلاح المشكلة مباشرة
- استخدم `browser_eval` للتحقق من الإصلاح

### 4. التحقق من قاعدة البيانات
- استخدم Supabase MCP tools للتحقق من البيانات
- راجع Advisors بانتظام للأمان والأداء

## 📋 مثال على سير العمل

```javascript
// 1. اكتشاف الخادم
nextjs_index()

// 2. الحصول على الأخطاء
nextjs_call({ port: "3000", toolName: "get_errors" })

// 3. إذا كان هناك خطأ، البحث عن الحل
nextjs_docs({ action: "search", query: "error message" })

// 4. إصلاح المشكلة في الكود

// 5. التحقق من الإصلاح
browser_eval({ action: "navigate", url: "http://localhost:3000" })
browser_eval({ action: "console_messages", errorsOnly: true })
```

## ⚙️ الإعدادات

### next.config.js
تم إضافة تعليقات توضيحية لـ MCP في `next.config.js`:
- MCP متاح تلقائيًا في Next.js 16+
- لا حاجة لإعدادات إضافية

### .cursorrules
تم إنشاء ملف `.cursorrules` مع:
- إرشادات استخدام MCP tools
- سير العمل للتطوير الديناميكي
- أفضل الممارسات

## 🎯 المزايا

1. **اكتشاف الأخطاء التلقائي**: لا حاجة لانتظار الأخطاء، يتم اكتشافها فورًا
2. **الحلول الديناميكية**: استخدام الوثائق الرسمية للحلول المحدثة
3. **التحقق الفوري**: استخدام browser automation للتحقق من الإصلاحات
4. **مراقبة قاعدة البيانات**: استخدام Supabase MCP tools للمراقبة
5. **لا حاجة لإعادة التشغيل**: معظم الإصلاحات تعمل مع Fast Refresh

## 📝 ملاحظات مهمة

1. **Next.js 16+**: MCP متاح تلقائيًا في Next.js 16+
2. **Dev Server**: يجب تشغيل `npm run dev` لاستخدام MCP tools
3. **Fast Refresh**: معظم التغييرات تعمل مع Fast Refresh دون إعادة تشغيل
4. **الوثائق**: استخدم `nextjs_docs` دائمًا للحصول على معلومات محدثة

## 🔄 الخطوات التالية

1. شغّل `npm run dev`
2. استخدم `nextjs_index` لاكتشاف الخادم
3. ابدأ التطوير مع مراقبة الأخطاء التلقائية
4. استخدم MCP tools لحل المشاكل ديناميكيًا

---

**تم الإعداد بنجاح! 🎉**

MCP جاهز الآن للتطوير الديناميكي وحل المشاكل دون انتظار.

